#include <iostream>
#include <vector>
#include <string>

using std::cout;
using std::endl;
using std::string;
using std::vector;

int main () {
  // Allows methods to set floating-point-precisionn.
  cout.setf(std::ios::fixed);
  cout.setf(std::ios::showpoint);


}
